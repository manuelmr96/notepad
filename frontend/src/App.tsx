// Phase 1 placeholder shell. RouterProvider + auth/notes screens are added in
// Plan 05 (auth) and Plan 06 (notes shell). This div confirms the Tailwind v4
// pipeline applies utilities end-to-end.
export default function App() {
  return (
    <div className="flex min-h-svh items-center justify-center bg-background text-foreground">
      <p className="text-sm text-muted-foreground">
        Notepad — frontend scaffold ready.
      </p>
    </div>
  );
}
